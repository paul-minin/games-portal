# Cookie Clicker (einfach)

Ein kleines Demo-Projekt (HTML/CSS/JS) — ideal zum Hosten mit **GitHub Pages**.

## Dateien
- `index.html` – Hauptseite
- `style.css` – Styles
- `app.js` – Logik (localStorage)

## Lokal testen
Öffne `index.html` im Browser (Doppelklick).

## Auf GitHub hosten (kurzanleitung)
1. Erstelle ein neues Repository auf GitHub (z. B. `cookie-clicker`).
2. In deinem Ordner `test2` (lokal):
```bash
git init
git add .
git commit -m "Add cookie clicker"
git branch -M main
git remote add origin https://github.com/<username>/<repo>.git
git push -u origin main
```
3. Gehe auf GitHub → Settings → Pages, wähle Branch `main` und Ordner `/ (root)` → Save.
4. Nach kurzer Zeit ist die Seite erreichbar unter `https://<username>.github.io/<repo>/`.

Viel Spaß! 🍪